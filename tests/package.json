{
  "name": "tests",
  "version": "1.0.0",
  "description": "Playwright tests for API hosted on Azure Kubernetes",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ci": "npx playwright install --with-deps && playwright test"
  },
  "keywords": [],
  "author": "Jyotirmay Sarna",
  "license":"Do not copy, use or share all or whole of this code, without permission",
  "Dependencies": {},
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "allure-playwright": "^3.10.0"
  }
}
